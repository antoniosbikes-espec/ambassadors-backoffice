# ── Netlify config ────────────────────────────────────────────
[build]
  command = ""
  publish = "."

[build.environment]
  PYTHON_VERSION = ""

# Redirigir todas las rutas al index.html (SPA)
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
